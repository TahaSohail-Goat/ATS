/**
 * Shared Tailwind theme fragment (non-color tokens). apps/web layers this
 * with the ATS color tokens from packages/ui — see
 * apps/web/tailwind.config.ts.
 */
export const baseTheme = {
  borderRadius: {
    sm: '0.25rem',
    md: '0.375rem',
    lg: '0.5rem',
    xl: '0.75rem',
  },
};
