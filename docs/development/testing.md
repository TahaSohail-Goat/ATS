# Testing Strategy

## Type checking and Build verification

```bash
npm run typecheck
npm run build
```

Verify that TypeScript compiles with 0 errors and Vite generates the production bundle in `dist/`.
