# Backups

Database backups are the responsibility of the managed Postgres provider
chosen for staging/production (provider TBD — see
`../architecture/deployment-architecture.md`). Once a provider is selected,
document here: backup frequency, retention period, and restore procedure/
RTO/RPO targets.

**TBD:** finalize once hosting provider is chosen.
