# Deployment Monitoring

Monitor the deployed frontend for:

- homepage and public-route availability;
- JavaScript errors and failed asset requests;
- Core Web Vitals, especially LCP, CLS, and INP;
- contact-provider delivery separately through the provider dashboard.

There is no local API health endpoint in the current frontend-only phase.
