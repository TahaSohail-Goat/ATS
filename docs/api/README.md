# API Documentation

- [`conventions.md`](./conventions.md) — REST conventions, response
  envelope, error format, pagination, status codes.
- [`authentication.md`](./authentication.md) — auth approach (not yet
  implemented; documents the plan and constraints for when it is needed).

Base path: `/api/v1`. OpenAPI docs are served at `/api/v1/docs` in
non-production environments (see `../architecture/api-architecture.md`).
