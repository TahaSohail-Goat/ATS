# Public site URL used for metadata and canonical links.
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional hosted form endpoint (Formspree, Basin, FormSubmit, etc.).
# Leave empty locally; configure it in the deployment environment before
# accepting enquiries. No ATS API or database is required.
NEXT_PUBLIC_CONTACT_FORM_ENDPOINT=
