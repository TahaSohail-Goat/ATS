import { CtaSection } from '../../components/CtaSection';

/** Home final CTA — dark contrast band closing the conversion path. */
export function FinalCta() {
  return (
    <CtaSection
      title="Have an idea? Let's build it together."
      description="Tell us what you're building — we'll reply with honest technical feedback and a clear first step."
    />
  );
}
