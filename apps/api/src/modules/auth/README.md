# auth module (placeholder)

No authentication is implemented in v1 — see `docs/product/scope.md` and
`docs/api/authentication.md`. This directory is a structural placeholder
so the module location is predictable when auth is scoped; it intentionally
contains no code yet. Do not implement partial auth here without the ADR
required by root `AGENTS.md` §9.
